<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Reservaciones</title>
    <link rel="icon" href="/icon/Icon.png">
    <link rel="stylesheet" href="reservaciones.css">
</head>

<body>
    <%- include('layouts/header.ejs') %>
    <header class="header">
        <h1 class="titulo-principal">Reserva tu Estancia Perfecta</h1>
        <p class="subtitulo">Elige entre nuestras habitaciones exclusivas y vive una experiencia inolvidable</p>
    </header>
    <main class="main-content">
        <section class="habitaciones">
            
            <% habitaciones.forEach(function(habitacion) { %>
                <div class="habitacion">
                    <img class="imagen-habitacion" src="<%= habitacion.enlace %>" alt="Imagen de <%= habitacion.nombre %>">
                    <h2><%= habitacion.nombre %></h2>
                    <p><%= habitacion.descripcion %></p>
                    <p class="precio">$<%= habitacion.precio %>/noche</p>
                    <button class="btn-reservar" data-id="<%= habitacion.id %>">Reservar</button>
                </div>
            <% }) %>
            
            

        </section>

        <!-- Formulario de Reservación -->
        <section class="formulario-reserva">
            <h2>Califícanos</h2>
            <form id="formulario-reserva" action="/reservaciones" method="POST">
                <label for="nombre">Nombre:</label>
                <input type="text" id="nombre" name="nombre" placeholder="Tu nombre completo" required>
                <span id="error-nombre" class="error"></span>
                
                <label for="correo">Correo:</label>
                <input type="email" id="correo" name="correo" placeholder="Tu correo electrónico" required>
                <span id="error-correo" class="error"></span>
                
                <label for="telefono">Número de teléfono:</label>
                <input type="tel" id="telefono" name="telefono" placeholder="Tu número de teléfono" required>
                <span id="error-telefono" class="error"></span>
                
                <label for="mensaje">Mensaje:</label>
                <textarea id="mensaje" name="mensaje" placeholder="Escribe tus comentarios o peticiones" required></textarea>
                <span id="error-mensaje" class="error"></span>
                
                <button type="submit" class="btn-enviar">Enviar</button>
            </form>
            <div id="respuesta"></div>
        </section>
        
    </main>

    <%- include('layouts/footer.ejs') %>
    <script src="reservaciones.js"></script>
</body>
</html>
